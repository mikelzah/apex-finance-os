# Собирается скриптом и выкладывается из Actions. В репозитории не хранится:
# копия рядом с исходником неизбежно разошлась бы с ним, и однажды
# опубликовалась бы старая версия.
/deploy/

# Вывод проверок: логи и снимки экрана. Плодится при каждом прогоне.
/tests/.out/
/tests/.pages/

# Ставится из package.json, в репозитории не хранится.
/node_modules/
